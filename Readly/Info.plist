<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<!-- Sparkle only. Every other key is still generated from build settings
	     (GENERATE_INFOPLIST_FILE stays on and merges into this file), because
	     Sparkle reads SUPublicEDKey straight from the bundle and Xcode's
	     INFOPLIST_KEY_ passthrough silently drops keys it does not know. -->
	<key>SUFeedURL</key>
	<string>https://raw.githubusercontent.com/mberrishdev/Readly/main/appcast.xml</string>
	<!-- Public half of the EdDSA signing key, printed by
	     scripts/generate-sparkle-keys.sh. The matching private key lives in
	     the login Keychain on the machine that ran that script and is never
	     committed here. -->
	<key>SUPublicEDKey</key>
	<string>fJ+7Z3yZBPNXBJd6ogkoh34ZsbMAiawDZbeF5H47M4A=</string>
	<key>SUEnableAutomaticChecks</key>
	<true/>
	<!-- Once a day. Readly is a menu-bar app people leave running for weeks,
	     so anything shorter is noise. -->
	<key>SUScheduledCheckInterval</key>
	<integer>86400</integer>
	<!-- Downloading happens only when the user opts in; the default stays off
	     so an update never replaces the binary unasked. -->
	<key>SUAllowsAutomaticUpdates</key>
	<false/>
</dict>
</plist>
